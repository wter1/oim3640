# Session 10/11 (2/24-26) - [Strings/Quiz1]

## What I learned today
- Strings are **immutable!!!** so something like string[0] = "l" will NOT work (you can try concatenating with + instead)
- Strings are **sequences**, which makes me think of tin cans lined up in a row and when you do something like: string[0] (for index 0) I'll *look* at or select the 0th character
- Strings can be **sliced** because they are a sequence using the ":" kinda like in r with some objects
- Many **methods** can be applied to strings here's just a list from the slides:
    - .upper()
    - .lower()
    - .startswith("hello")
    - .count("a") #This one is NOT like len(), pretty sure it just counts how many instances of a given argument
    - .replace("a", "o") #replaces first letter with second letter for all instances, this creates a new string and does **NOT** change the original

## Code/work I'm proud of (optional)
def check_word(word, available, required):
    """Check whether a word is acceptable.
    
    >>> check_word('color', 'ACDLORT', 'R')
    True
    >>> check_word('ratatat', 'ACDLORT', 'R')
    True
    >>> check_word('rat', 'ACDLORT', 'R')
    False
    >>> check_word('told', 'ACDLORT', 'R')
    False
    >>> check_word('bee', 'ACDLORT', 'R')
    False
    """
    if len(word) >= 4 and uses_only(word, available) == True and uses_all(word, required) == True: #It would be redundant to make new conditional statements when we can just use the ones we already created
        return True
    return False

def score_word(word, available):
    """Compute the score for an acceptable word.
    
    >>> score_word('card', 'ACDLORT')
    1
    >>> score_word('color', 'ACDLORT')
    5
    >>> score_word('cartload', 'ACDLORT')
    15
    """


    if len(word) == 4:
        score = 1
    else:
        score = len(word)
    
    is_panagram = True
    for letter in available.lower():
        if letter not in word.lower():
            is_panagram = False
            break
    
    if is_panagram:
        score += 7 
    
    return score

The above is code done in chapter 7 for the spelling bee

## Challenges I faced
I don't think strings by themselves will give me any issues, it's just when they're combined with other things like loops.
I feel like I still don't know every interaction a string has with the python language, though I guess I'll figure it out along the way.
Also the doctests were kind of confusing to use

## AI usage (if any)
creating doctests for me to use

## Questions for next time
are we going to use doctests in class? I don't recall it ever coming up other than in the chapter.



Claude opus 4.6
OCR