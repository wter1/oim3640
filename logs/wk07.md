# Session 12/13 (3/3-5) - [Lists]

## What I learned today
I was sick this week and couldn't attend class, but the chapter and Professor Li's slides were really helpful!
- Lists are **Mutable!!!** stocks[2] = "NVDA" works to replace the second value in a list
- Lists, like strings are also a **sequence** and functions the same as strings, except every returned value is an entire variable instead of just 1 character.
- **Sliced** lists return a new list (This is not an alias)
- It also has **methods** here's a *list*:
    - .append("TSLA")
    - .extend(["NVDA", "AMZN"])
    - .pop() #just removes last value in list like poping a tin can down at the end!
    - .remove("META") #Removes first meta. That was from the slides, by "first" I assume if there was another "META" after the first instance, it would not be removed. 
- Neat **conversions**:
    - string_variable.split(",") for *String* to *List*
    - ",".join([list here]) for *List* to *String*
- **Aliasing** a list can be done y literally assigning the original list variable to a new variable. This will be the same list and NOT a copy (refer to chapter 9 for that one visual picture)

## Code/work I'm proud of (optional)
N/A

## Challenges I faced
I think the most difficult thing for me was (and still kind of is) is the idea of mutability and immutability. I think it's just like an internal mental thing because I know it's the ability to change (or not), but I think seeing id's of the variables really helped me understand the difference between the two more 

## AI usage (if any)
None that I can recall, the chapter and slides were sufficient.

## Questions for next time
Both lists and strings are sequences, I wonder if there will be other "sequences" introduces since I see that dictionary and tuples are up next